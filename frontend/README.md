# Trustora Frontend

React + Vite + TypeScript interface untuk Trustora.

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

Set API backend lewat `.env`:

```env
VITE_API_BASE_URL=http://localhost:8000
```

